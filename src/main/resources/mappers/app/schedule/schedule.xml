<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hiair.app.schedule.mapper.ScheduleJobMapper">
	
	<!-- schedule job list 조회 -->
    <select id="select" resultType="scheduleJobVO">
		SELECT	seq,
		    	jobName,
		    	triggerName,
		    	description,
		    	executeMethod,
		    	batchType,
		    	cronExpression
		FROM 	SCHEDULE_JOB
		ORDER BY seq ASC
  	</select>
  	
  	<!-- schedule job 수정 -->
	<update id="update" parameterType="scheduleJobVO">
		UPDATE 	SCHEDULE_JOB 
		   SET	cronExpression = #{cronExpression}
		   	    ,description = #{description}
		WHERE 	jobName = #{jobName}
		AND 	triggerName = #{triggerName}
  	</update>

</mapper>
